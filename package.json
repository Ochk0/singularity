{
  "name": "singularity",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run build:wasm && vite",
    "build": "npm run build:wasm && vite build",
    "preview": "vite preview",
    "build:wasm": "asc wasm/matter.ts --config asconfig.json --target release",
    "build:wasm:debug": "asc wasm/matter.ts --config asconfig.json --target debug"
  },
  "dependencies": {
    "lil-gui": "^0.20.0",
    "three": "^0.171.0"
  },
  "devDependencies": {
    "assemblyscript": "^0.28.15",
    "vite": "^6.0.7"
  }
}
